# Raw datasets — do not modify these files
# Place your three source datasets here:
#   - budgetwise_finance.csv       (BudgetWise Finance dataset)
#   - cards_transactions.csv       (Cards dataset)
#   - personal_finance.csv         (Personal Finance dataset)
#
# These are git-ignored (see root .gitignore).
# Phase 2 will ingest these into PostgreSQL.
